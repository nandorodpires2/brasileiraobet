<section>    
    <div class="row">
        <!-- SIDE BAR -->
        <div class="col-md-3">            
            <?php if (Plugin_Auth::check()) : ?>
            
            <!-- BOX MINHA CONTA -->
            <?php echo $this->partial('index/box-minha-conta.phtml', array(
                'notificacoes' => $this->notificacoes
            )); ?>
            
            <!-- BOX SALDO -->
            <?php echo $this->partial('index/box-saldo.phtml', array(
                'saldo' => $this->saldo,
                'saldo_resgate' => $this->saldo_resgate,
                'lancamentos' => $this->lancamentos
            )); ?>
            
            <!-- BOX APOSTAS -->
            <?php echo $this->partial('index/box-apostas.phtml', array(
                'apostas' => $this->apostas,
                'formApostar' => $this->formApostar,
                'formApostarAlterar' => $this->formApostarAlterar                
            )); ?>
            
            <?php else : ?>
            
            <!-- BOX LOGIN/CADASTRO -->
            <?php echo $this->partial('index/box-auth.phtml', array(
                'formLogin' => $this->formLogin
            )); ?>
            
            <?php endif; ?>
        </div>
        
        <!-- PARTIDAS -->
        <?php echo $this->partial('index/box-partidas.phtml', array(
            'partidas' => $this->partidas,
            'saldo' => $this->saldo,
            'formApostar' => $this->formApostar
        )); ?>
        
    </div>
</section>
